{
  "name": "aiuxlab-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "AI Research POC monorepo",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "start": "pnpm --filter web start",
    "lint": "pnpm --filter web lint",
    "lint:fix": "pnpm --filter web lint:fix",
    "format": "pnpm --filter web format",
    "format:check": "pnpm --filter web format:check",
    "type-check": "pnpm --filter web type-check",
    "check": "pnpm --filter web check",
    "fix": "pnpm --filter web fix",
    "generate:api": "pnpm --filter web generate:api",
    "generate:watch": "pnpm --filter web generate:watch",
    "test": "pnpm --filter web test",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "vitest": "^3.2.4"
  }
}
